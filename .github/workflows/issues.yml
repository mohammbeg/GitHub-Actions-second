name: Output Information
on: issues
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output Event Information
        run: echo "${{ toJSON(github.event) }}" # git hub expressions